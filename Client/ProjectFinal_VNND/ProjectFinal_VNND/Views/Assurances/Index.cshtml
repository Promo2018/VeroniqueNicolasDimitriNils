@model IEnumerable<ProjectFinal_VNND.Models.Assurances>
    @using ProjectFinal_VNND.Models; 


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Ajouter une Assurance", "Sauvegarder")
    </p>

    @using (Html.BeginForm())
    {
        <p>
            Rechercher: @Html.TextBox("type", "", new { placeholder = "Type de l'assurance ..." })
            <button type="submit"><i class="fa fa-search"></i></button>
        </p>
    }

    <table class="table">
        <tr>
            <th>
                Type d'Assurance
            </th>
            <th>
                Coût (en % du prix Total) 
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.libelle)
                </td>
                <td>
                    @item.prix100
                </td>
                <td>
                    @Html.ActionLink("Modifier", "Modifier", new { id = item.id_assurance }) |
                    @Html.ActionLink("Informations", "Informations", new { id = item.id_assurance }) |
                    @Html.ActionLink("Supprimer", "Supprimer", new { id = item.id_assurance })
                </td>
            </tr>
        }

    </table>
</body>
</html>
