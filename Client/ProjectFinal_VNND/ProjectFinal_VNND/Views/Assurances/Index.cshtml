@model IEnumerable<ProjectFinal_VNND.Models.Assurances>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.libelle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descriptif)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.libelle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descriptif)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_assurance }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_assurance }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_assurance })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
