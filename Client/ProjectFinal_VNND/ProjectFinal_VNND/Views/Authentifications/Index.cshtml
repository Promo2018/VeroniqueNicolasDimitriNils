@model IEnumerable<ProjectFinal_VNND.Models.Authentifications>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Ajouter Utilisateur", "Sauvegarder")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Statuts.statut)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Statuts.statut)
                </td>
                <td>
                    @Html.ActionLink("Modifier", "Modifier", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Informations", "Informations", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Supprimer", "Supprimer", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }

    </table>
</body>
</html>
